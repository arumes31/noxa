# noXa role permissions: implementation checklist

Status: implementation in progress. Design: `tasks/plan.md`. This release starts on an empty PostgreSQL database; production entrypoints reject unmarked existing databases. Startup requires active roles-v1 authority and the desktop requires a roles-v1 handshake. Dated checklist entries below include historical staging notes. No production launch is authorized by checking off a development task.

Detailed current progress, validation and remaining cleanup: `tasks/permission-implementation-status.md` (updated 2026-09-22). Items below remain open until their listed verification is complete.

## 1. Freeze the capability and enforcement inventory
- [x] Audit advertised role capabilities against current handlers and protected delivery; remove unimplemented invitation grants, disclose invisible-member authority, and fix active media flags surviving denied channel transitions. Evidence and remaining test gaps: `docs/capability-enforcement.md`.
- [x] Reconcile all 65 declared legacy keys with dispositions and record remaining authority families, including the unresolved raw native channel-mutation boundary. Reproduce and fix hidden/invisible member-ID disclosure through mention lists; verify sender visibility, recipient-only notification metadata and live grant revocation.
- [ ] Map every existing permission key, handler check and legacy admin bypass to a named capability, setting, identity attribute or retirement decision. Include TCP, Query/SSH, gRPC, guests, bot identity, file transfer and encryption-key entitlement.
- [ ] Define capability scope/prerequisites, owner/Admin boundaries and before/after scenarios in executable fixtures; identify any existing feature with no planned replacement.
- [ ] Verify: review inventory against searches for PermissionKey, power checks, isAdmin, LoadForClient and protocol permission commands; no unexplained entries remain.

Dependencies: none. Scope: medium. Likely files: a new catalog/design fixture, `internal/permissions/types.go`, related permission tests.

## 2. Implement and prove the pure role evaluator
- [x] Resolve role unions, effective channel overrides, member exceptions and explanations with the documented conflict rules.
- [x] Implement separate hierarchy/prerequisite checks; reject unknown or out-of-scope keys without inventing defaults.
- [x] Verify: focused table/property tests for conflicting roles, equal rank, missing policy, owner/Admin and invariance under role evaluation order.

Dependencies: 1. Scope: medium. Likely files: `internal/permissions/types.go`, `resolver.go`, new role resolver and hierarchy test files.

## Checkpoint A: semantics
- [ ] Review concrete examples for moderator delegation, channel exceptions and multiple roles; evaluator fixtures match the proposed UI explanations.

## 3. Persist roles and protected ownership
- [x] Add role/grant/member tables plus owner/model/revision state without activating the replacement for current sessions.
- [x] Provide transaction helpers enforcing owner integrity, @everyone invariants, role ordering and revision conflicts.
- [ ] Verify: real PostgreSQL constraints, concurrent reorder/assignment and owner-protection tests; existing schema still starts before activation.

Dependencies: 2. Scope: medium. Likely files: one new numbered migration, proposed `internal/store/roles.go` and its tests, server dependency contracts.

## 4. Deliver an acknowledged role-management API
- [x] Create/edit/delete/reorder and assign/remove roles using pre-change authority, target hierarchy and expected_revision.
- [x] Atomically audit mutations; only owner can grant Administrator; every operation returns committed state or a structured error.
- [ ] Verify: direct protocol tests for self-escalation, stale targets/revisions, role deletion and rollback on failure.

Dependencies: 3. Scope: medium. Likely files: `internal/netproto/codec.go`, proposed server role handlers/tests, store role methods.

## 5. Deliver the Roles editor
- [x] Add native client bindings and a role list/editor with template creation, permissions search, color/icon, Save/Discard and keyboard ordering.
- [x] Display server conflict/denial results without changing authoritative UI state on failure; include English/German copy.
- [ ] Verify: unit and browser create/edit/reorder/conflict workflows, generated-binding consistency and frontend lint/build.

Dependencies: 4. Scope: medium; generated bindings are outputs. Likely files: proposed `client/roles.go`, `roles-ui.js`, role messages/styles and browser tests.

## Checkpoint B: role administration
- [ ] A fixture owner creates a Moderator role and a lower manager cannot change or assign it; changes appear only after acknowledgement.

## 6. Deliver member-role assignment
- [ ] Add role chips and searchable assignment to Members, including batch results and consistent checks for offline accounts.
- [x] Replace role-based member colors/hoisting presentation; prevent cosmetic ordering from changing access precedence. Filtered snapshots carry assigned public cosmetics only, and open member profiles clear revoked roles.
- [ ] Verify: member assignment/removal, failed mixed batch, guest restrictions and stale-dialog/server-switch workflows.

Dependencies: 5. Scope: medium. Likely files: role UI/bindings, member presentation, browser/server tests.

## 7. Store and edit channel access
- [x] Add role/member override storage and explicit synced/custom state with foreign keys and validation.
- [x] Implement atomic channel tree/policy storage and isolated lifecycle reconciliation, including retry-safe media detachment and system-audited temporary-leaf cleanup. Keep production startup unchanged until cutover is complete.
- [x] Implement atomic acknowledged channel create/edit/move/delete, cycle/subtree validation and explicit keep/sync access choice with native bindings and localized dialogs.
- [x] Add read-only, revision-bound before/after member-access previews for existing-channel drafts and resync, using the same authorization and evaluator as saving. Require exact-draft review before save; include a guest baseline and paginated registered-member results with explicit scope, English/German controls and stale-result protection.
- [x] Extend detailed member-impact review to moves and new channels, with exact-draft review for custom creation and synced moves, no stored preview IDs, and inherited creation available to temporary creators without roster access.
- [x] Add explicit synced-descendant impact scopes to access and move drafts, with authorized scope discovery, scoped rosters, response identity checks and review invalidation when selection changes.
- [x] Verify preset/sync/custom/resync privacy with a shared frontend/server contract: four presets, granted and empty baselines, seven transitions, guests/member exceptions/owner/Admin, synced descendants, custom independence and exact preview parity.
- [ ] Verify: PostgreSQL and server tests for conflicting overrides, missing subjects, parent updates, resync, moves and concurrent edits.

Dependencies: 4. Scope: medium. Likely files: access migration, proposed store/server access files and tests, protocol contracts.

## 8. Deliver Channel access UI and presets
- [x] Build parent-sync status, Inherit/Allow/Deny controls and Public/Private/Read-only/Listen-only presets using ordinary overrides.
- [ ] Show exceptions, unsaved changes and resync impact; localize all states and expose accessible keyboard controls.
- [x] Verify browser channel creation/presets and keep/sync move, acknowledged edit/delete, pending enforcement, stale server menus/replies, Unicode limits and keyboard selector navigation (12 channel lifecycle workflows).
- [x] Verify the preset/sync/custom/resync privacy matrix against pure evaluator decisions and exact member-impact diffs; browser creation payloads match the shared four-preset contract.

Dependencies: 7, 5. Scope: medium. Likely files: client access bindings, proposed access UI/messages/styles, workflow tests.

## 9. Deliver Check access
- [x] Return an authorized explanation for a selected member/channel using the same evaluator and revision (production enforcement integration remains Task 10 onward).
- [x] Display grant sources, prerequisite denials, checked revision and actor-to-subject hierarchy separately; a read-only member picker includes protected/higher members. Preview never fetches protected content or impersonates the target.
- [x] Verify: explanations match guest/member/owner decisions and prerequisites over TCP, unauthorized/scoped/stale previews are rejected, and English/German browser workflows cover keyboard use, protected-member selection and stale-result rejection.

Dependencies: 7, 6. Scope: medium. Likely files: authorization preview handler/binding/UI and focused tests.

## Checkpoint C: manageable UI
- [ ] Run the novice-admin task script; private channel, read-only channel and conflicting-role diagnosis work in English/German with keyboard access.

## 10. Enforce channel visibility and chat access
- [ ] Route snapshots, channel lists, subscriptions, send/history/search and encrypted key distribution through explicit target-channel authorization.
- [ ] Deny hidden-resource lookup without leaking names/content; define global chat and direct-message policy separately from channel inheritance.
- [ ] Verify: multi-client integration tests for forged IDs, subscribed-but-not-joined chat, historical keys and revoked channel access.

Dependencies: 2, 7. Scope: medium per seam; split implementation commits for visibility, chat and key distribution. Likely files: `internal/server/handlers.go`, `subscriptions.go`, `chatx.go`, `e2e.go` and focused tests.

## 11. Enforce voice, video and file access
- [x] Use an independent uploader quota setting in role mode and recheck channel/uploader ceilings at upload finalization, preserving dedup accounting; verify overlapping uploads, fail-closed usage lookup and unchanged versions after denial.
- [x] Apply the backend uploader ceiling to cross-channel moves and prevent caller quota overrides; account for retained/last source references and reject destination lookup failures before filesystem/database effects. Verify real PostgreSQL accounting and move regressions.
- [x] Make deleted-channel icon cleanup retryable before state removal, preserving unrelated icons.
- [x] Add explicit bot account metadata across authentication and operator provisioning; prove legacy bot/admin flags grant no role-mode authority.
- [ ] Replace power checks with Connect/Speak/publish and file capabilities in target scope; preserve server mute/deafen, passwords and resource limits as separate rules.
- [x] Add an independent optional startup video relay bitrate ceiling across each publisher's camera/screen/simulcast packets, including recorder taps. Verify RTP bytes, burst/refill, separate publishers, concurrency, moves/ICE and cleanup; keep resolution/client/native-media work explicit in docs/media-resource-limits.md.
- [x] Add optional startup encoded width/height limits with matching VP8-only negotiation and per-track keyframe inspection. Verify malformed/lost/oversized frames, current codec changes, recovery PLI, reference/track reset and actual SDP; fuzz the inspector. Native capture constraints/feedback, runtime settings UI and end-to-end recording remain.
- [x] Add atomic live router bitrate/dimension updates that drain earlier output writes, reject stale inspections and reset per-track bounds generations. Preserve no-op/invalid updates and bitrate-only reference state; load persisted server defaults before media construction. Engine renegotiation, bounded update failure handling, client updates and runtime settings/editor wiring remain open.
- [x] Replace engine codec configuration for new/rebuilt peers and coordinate it with live router limits through Voice.SetVideoLimits. Preserve shared ICE transport, certificate identity, operator AV1 preference and existing peers; verify real rebuilt/server-initiated SDP, local max-fs advertisement, codec-slice ownership, concurrent updates and invalid/closed/no-op cases. Automatic client rebuild/capture/control restoration, bounded update failure handling, downstream buffers, persistence and the editor remain open.
- [x] Add versioned media-limit notifications (163) and an optional authentication revision baseline. Validate complete values, reject stale revisions and obsolete transports, retain native per-tab snapshots, and emit payload-free invalidations. Verify raw malformed TLS authentication, live TLS updates, full-width revisions, background tabs, pending requests and source-replacement races. Server production publication, frontend capture/rebuild handling and management persistence/editor remain open.
- [x] Add server post-commit media-limit publication and order authentication baselines under the per-client writer lock. Queue current-state refresh markers; close failed queues and enforce a pending-delivery deadline across backlog and writes. Verify real legacy/role TLS, login races, pre-response readiness, queue failures, revision limits and stale timer callbacks. Production apply/persist orchestration, frontend capture/rebuild handling and editor remain open.
- [x] Add the frontend capture/encoder update primitive, preserving live sources for bitrate changes and pausing/checking dimension adjustments. Revalidate pending camera/screen publication against latest limits; contain obsolete completions and errors. Verify real Chromium sender caps, delayed constraints/caps/queues, superseding updates, replacement sessions and failure cleanup. Native invalidation handling, bounded peer rebuild/control restoration, persistence and editor wiring remain open.
- [x] Connect scoped live media invalidations to current native snapshots, capture updates and dimension-triggered peer rebuilds. Bound startup limit reads and whole live refreshes; cancel startup ownership immediately on reset. Preserve server-side whisper/quality intent across transport rebuilds, recreate guarded whisper tracks and renegotiate affected subscribers. Verify stale/late events, startup races, queue/constraint/offer failures and timeouts, current controls/guards, failed rebuild retries and final cleanup. Server apply/persistence, bounded media writers/downstream buffers, editor and physical-media rehearsal remain open.
- [x] Add cancellable engine/output/update gates and coordinated prepare/persist/apply/publication for media limits. Preserve runtime policy on failed saves, finish confirmed commits despite request cancellation, and load all media settings atomically before media construction. Verify stalled output/update/engine waits, old inspection rejection, queued peer creation, PostgreSQL rollback/restart, invalid/empty/sealed settings and publisher budgets. Authorized management transport/editor wiring, termination of stalled outputs and downstream transport buffers remain open.
- [x] Advertise and validate per-connection media limits, apply bounded native-client capture and shared encoder budgets, and display localized limits/failure feedback. Verify real TLS, tab/session isolation, combined layers/sources, concurrent publication, rejected caps and immediate teardown; retain runtime editing and physical capture/FFmpeg rehearsal as remaining work.
- [ ] Implement active-session revocation: stop unauthorized media, stop further protected file operations and rotate scope keys where entitlement changes.
- [ ] Verify: live publish/receive revocation, upload/download after role removal, whisper scope and native media tests.

Dependencies: 10. Scope: medium per media/files slice; implement separately if necessary. Likely files: `internal/server/voice.go`, `files.go`, `perms.go`, media integration and tests.

## 12. Enforce moderation and delegation everywhere
- [x] Bind file-browser upload bytes/path, download/retry and cancellation to the source native tab. Preserve picker channel/folder, isolate pending upload ownership across reset, scope transfer rows, and validate return-to-tab retry targets. Remaining chat attachment/session bindings still need completion.
- [x] Retain coalesced native progress per tab with all active and 20 recent finished transfers, replay an authoritative snapshot during activation, remove expired frontend rows/retries and recover background failure state. Verify concurrent activation, closed tabs, empty snapshots, independent channel snapshots and return-to-tab Resume.
- [x] Bind channel/global chat history and pin reads to the originating native tab and encryption manager. Reject stale frontend results by server/cache/panel/request identity; preserve newer pin snapshots and live deltas during preload. Verify native activation gaps, in-flight key isolation, delayed history/pin results/errors and unrelated pin preservation. Other chat mutations/search/export/attachment/session bindings remain unfinished.
- [x] Bind search/export scans to the originating tab through all pages and correlate progress by request ID. Invalidate obsolete conversation attempts, search busy state/results and export progress; preserve confirmed transcript contents and explicit plain/encrypted save choices. Verify same-server round trips, DM result scope, overlapping progress and late native save feedback. Already-started native scans/saves may finish on their captured source; mutations, attachments and other session bindings remain.
- [x] Bind message edits, deletion, reactions and pin/unpin writes to the originating tab. Preserve edit encryption/version metadata, invalidate obsolete confirmations and feedback, guard reaction continuations and pin-panel ownership, and dismiss old controls on navigation. Verify native payloads/stale-tab rejection and browser activation, round trips, bridge failures and detached controls. Sending, attachments, receipts/typing, local DM history/session callbacks and committed chat-write acknowledgements remain.
- [x] Bind encrypted sends/replies and attachment upload/preview/save to the originating native tab. Capture reply/draft/destination before asynchronous work, serialize current-view sends, suppress obsolete follow-ups/retries/feedback, preserve newer same-value edits and reply choices, and invalidate staged files/readers/controls on navigation. Verify upload-init/save-dialog activation, encryption, native stale-tab rejection, browser retry/draft/reader/control behavior and adjacent chat workflows. Already-started captured transfers/saves may finish; receipts/typing, local DM history/session bindings and committed chat-write acknowledgements remain.
- [x] Bind typing and delivery/read receipts to the source tab. Capture typing timer scope, cancel on navigation, contain bridge failures and preserve exact visible read IDs in an owned per-peer queue. Flush only while chat is visible/focused, retry failures on later triggers and ignore replaced-peer completions. Verify native payloads/activation, browser visibility/focus/bursts/retries and adjacent DM workflows. Local DM history identity/callback binding and committed chat-write acknowledgements remain.
- [x] Capture one native DM storage identity per operation for filenames, encryption, peer enumeration and all search loads; capture before the append/clear writer lock. Preserve storage format, atomic replacement and nonblocking readers. Verify read/export activation, captured writes/peer enumeration, full client tests, DM races and lint. Expected frontend tab/identity contracts and stale DM callbacks remain unfinished.
- [x] Bind local DM history callbacks to reset generation and exact cache/peer ownership. Ignore old loads, peer scans, persistence warnings, clear confirmations/results and detached peer controls; preserve closed tabs during restoration. Serialize overlapping load/clear completion so deleted rows cannot reappear, retain later live rows, and contain clear bridge errors. Verify fifteen regressions plus adjacent chat workflows, frontend unit/lint and frontend/audio/native builds. Native expected-tab/identity validation and coordinated identity-change lifecycle remain unfinished.
- [x] Add native DM owner contexts with tab, identity UID and lossless activation/identity revisions. Validate every scoped history operation before DM disk effects, retain accepted stores through writer waits, reject tab/identity round trips and support real offline App-selected identity. Generate bindings and verify stale-operation, accepted-read/writer isolation, offline behavior, full client tests, DM races, lint and builds. Frontend adoption and coordinated identity-change/context-acquisition lifecycle remain unfinished; unscoped UI calls still exist.
- [x] Adopt frozen DM owner contexts for all frontend local-history operations, including pre-dialog clear/export capture. Coordinate monotonic identity notifications, pending/newer acquisition, explicit failure recovery and offline restoration; invalidate obsolete tabs/caches/typing/receipts/timers/quick-switcher actions. Preserve established connection keys/journal ownership and capture selected keys before new-tab publication. Publish authenticated UID before journal replay and use context UID for own-message routing. Verify native lifecycle/replay, thirteen new browser cases plus adjacent DM/chat/tab workflows (57 distinct), full client/frontend suites, race checks, lint and builds. Broader native group/session inventory and committed chat-write acknowledgements remain.
- [x] Bind file-browser listing, versions, download-link creation, checksum verification, rename/move and deletion to the initiating native tab. Guard late results, clipboard feedback, checksum timers and mutation refreshes with tab/channel/folder/view scope. Preserve existing server permissions and write semantics; native transfer queues/pickers/retries and committed file-write acknowledgements remain.
- [x] Bind legacy privilege-key list/create/revoke/redeem and its group lookup to the initiating native tab. Scope queued invite/raw credentials to their intended address, stop follow-ups after dialog closure, serialize manager mutations and ignore stale replies. Verify native payload/race isolation and browser activation, confirmation, offline routing and late results. Other group actions and the chat/file/session binding audit remain.
- [x] Add a committed native role ban-removal acknowledgement and matching tab-bound binding. Freeze the ban dialog until acknowledgement/reload and require explicit recovery after uncertainty. Verify database rollback/idempotence, current grants/revocation, cancellation, audit, wrong response IDs and native/browser session isolation. Other native moderation/asset acknowledgement contracts remain.
- [x] Bind channel drag/reorder and legacy create/edit/delete/tree calls to their originating tab. Preserve source generation, show a fresh acknowledged role editor, and commit optional move order atomically with parent/access/revision/audit. Verify explicit zero/omission, failed-audit rollback, Query/SSH/gRPC/native payloads, unavailable destinations, pending writes and server resets. Broader native binding inventory and committed legacy write acknowledgements remain.
- [x] Add role-editor channel icon upload/reuse/preview with a separate acknowledged native save. Verify target/source authorization, revoked access, storage failure, mismatched replies, preserved parent drafts, activation gaps, late preparation, uncertain-save refresh and compact German keyboard behavior.
- [x] Bind custom emoji list/image/upload/rename/delete across the native bridge, asset manager and chat picker. Guard delayed manager refreshes, contain rejected writes, close emoji/reaction popovers on reset and keep old image loops from attaching to replacement views. Validate native payloads, activation gaps, both upload flows, closed/reset managers and reaction message identity. Channel drag/reorder, broader native binding inventory and committed write acknowledgements remain.
- [x] Bind client/server metadata, MOTD/subscription recovery, channel icon reads/upload/copy, recent/quick-switch/notification channel navigation and leave-channel to their originating native tab. Discard late connection setup and metadata replies; preserve notification origin across tabs. Validate activation gaps, captured client identity, cached native isolation, stale browser results and existing navigation/polling behavior. Subsequent emoji, channel, icon-editor and token work is recorded above; other group/chat/file/session paths and committed write acknowledgements remain.
- [x] Bind member kick/ban/batch/poke and avatar/branding actions to the initiating native tab, expose role moderation without legacy powers, and suppress late session feedback. Bind channel-row joins and member drops; validate source generation/tab and exact session identity. Verify activation gaps, concurrent same-identity sessions, old drags, image pickers and native payloads. Other metadata/channel actions and committed write acknowledgements remain.
- [x] Scope shared audit/filter/ban/complaint dialogs to their opening native tab and use protected queries instead of legacy UI authorization flags. Prevent unloaded/overlapping filter saves, require reload after uncertain results and verify native/browser/server grant and stale-session behavior. Member actions, branding and acknowledged native ban removal were subsequently completed above; broader localization and native acknowledgement work remains.
- [x] Bind role/member/channel/access-preview/voice-moderation editors and the role audit viewer to their opening native tab. Reject activation-before-reset requests, preserve original-connection acknowledgements, and verify interrupted member batches and audit pagination. Continue the same audit for remaining legacy/shared management surfaces.
- [x] Make server settings rely on current server authorization in both models, with explicit reload after uncertain saves, validated/frozen controls and no protected search reads. Add native tab-bound query/save calls to reject old-server requests even before frontend reset; verify delegated grant/revocation over TCP, native activation gaps, stale browser replies and localization/accessibility.
- [x] Implement native role-mode server-kick revocation under an exclusive policy barrier, final canonical ban admission, queued-request rejection and guarded login completion; verify in-flight file/key effects, privacy, voice-error rotation and idempotent teardown. Account-wide bans and integration adapters remain separate.
- [x] Apply canonical account bans to every native session, preserve unrelated guests, normalize inaccessible targets, bound temporary durations and report unconfirmed persistence/saved pending cleanup. Suspend account recording taps before bounded stop/retry; validate PostgreSQL admission, audit, failures and recorder startup cancellation. Integration kick/ban adapters remain.
- [x] Filter audit targets under current policy using trusted persisted scopes; atomically record actual role/channel before/after state and render localized, safe, paginated audit entries. Verify legacy JSON provenance, hidden/deleted/mixed scopes, same-session revocation and unchanged resource identity.
- [x] Read login, reconnect and tab-replay identity/privilege/transport status from one tab-bound native session snapshot. Reject obsolete server/disconnect generations, preserve offline status and bound retries without duplicate recovery timers. Verify native concurrency/tab rejection and browser activation, replay, login, disconnect and retry flows. Other native bindings and committed operation acknowledgements remain.
- [x] Wait for committed role-mode edit/delete/pin/unpin/reaction acknowledgements. Validate operation/message identity, preserve captured server and legacy behavior, and render authoritative reactions without count/ownership duplication in either event/reply order. Verify blocked storage, failure/denial, malformed replies, native activation and model lifecycle. Message-send and other native write acknowledgements remain.
- [x] Wait for role-mode message acceptance: channel/global history persistence, live DM recipient queue acceptance and offline encrypted spool commit. Match the destination/reference, write acknowledged DM own echoes before success, preserve legacy behavior and retain tab-bound draft ownership. Verify held storage, failures, denial, malformed replies, existing deduplication and full sender queues. No automatic resend or exactly-once delivery is introduced; other native operation acknowledgements remain.
- [x] Wait for role-mode poke queue acceptance for the exact target on the captured connection. Return permission, target, cooldown and queue failures; retain legacy unrequested behavior and browser generation guards. Verify held/malformed/mismatched replies, tab switches, full/missing queues and obsolete UI errors. Kick/ban/move/join, assets and file mutation acknowledgements remain.
- [x] Acknowledge role-mode move, channel disconnect, server kick and account ban results on the captured native connection. Verify target/action/destination, distinguish saved versus unconfirmed bans and pending cleanup after revocation, preserve unrequested legacy behavior, and bound committed reply writer acquisition. Validate controlled effects, real PostgreSQL INSERT blocking/outcomes, native tab changes and browser batch/partial feedback. Native disconnect still needs displayed-channel preconditions; join, assets and file mutation acknowledgements remain.
- [x] Bind acknowledged native channel disconnects to the displayed source channel. Freeze single/batch target IDs and channels at menu-open, require the scoped tab API, compare source under the server target lock and validate the returned prior channel. Reject stale/malformed contexts without effects and verify a disconnect queued behind a move. Regenerate bridge and validate native/browser/legacy behavior. Join, asset/file acknowledgements and broader native binding inventory remain.
- [x] Wait for role-mode self-join and leave membership confirmation on the captured connection. Match the authenticated connection and explicit destination, including idempotent channel-zero leave; preserve existing permission/password/capacity checks and unrequested legacy behavior. Verify held effects, failures, Connect denial, state fallback, malformed replies and tab changes. Keep empty server errors from becoming native success, and validate browser pending/rejection behavior. Asset/file acknowledgements and broader native binding inventory remain.
- [x] Wait for role-mode avatar, server icon/banner and emoji upload/delete/rename storage confirmation. Match operation/session/names, reject requested legacy writes before effects and preserve unrequested behavior. Route older native channel-icon methods through the existing confirmed role API. Verify actual stored/deleted/renamed bytes and paths behind held asset locks, failures/denials, native tab changes and browser pending/success/rejection feedback. File mutation acknowledgements and broader native binding inventory remain.
- [x] Wait for role-mode file deletion and rename/move results on the captured connection. Require the explicit operation/session/source/destination tuple, preserve source ownership and destination authorization, and retain unrequested legacy behavior. Verify held effects, real PostgreSQL commit blocking, database failures, blob rollback, best-effort delete cleanup and native/browser tab isolation. Complete the remaining native session/group/API inventory before cutover.
- [x] Bind rules acceptance, rules decline, main/tray disconnect, channel subscriptions and avatar reads to the displayed server. Preserve event-authoritative rules/subscriptions; reject stale native tabs and suppress obsolete frontend completion. Verify same-server overlapping subscriptions and late failures after replacement. Record the remaining presence, media/voice and legacy-group seams in `docs/native-api-inventory.md`; full inventory/cutover remains unfinished.
- [x] Confirm role-mode presence updates and bind picker/auto-away actions to their opening/timer session. Validate exact reply fields, current invisible eligibility, no legacy-admin bypass, old/replaced intents, failed automatic-restore suppression and restored-tab idle timers. Preserve unrequested legacy behavior. Media/voice and legacy-group inventory and full cutover remain unfinished.
- [x] Bind ICE configuration, media limits and voice offers/answers/candidates to their initiating native tab. Preserve original-connection replies, guard queued camera/screen negotiation and late peer callbacks, and prevent old track cleanup from removing replacement attribution/playback. Remaining media controls, legacy-group inventory and full cutover remain unfinished; see docs/native-voice-session.md.
- [x] Bind priority/whisper/screen/received-quality controls to their native tab and confirm role-mode effects before reporting success. Validate denied and held effects, exact reply fields, whisper settings/hotkey ownership and retry, frozen reply targets, original-tab teardown and delayed PTT release. Preserve legacy submission semantics and local-only mute/PTT events; see docs/native-media-controls.md. Tray connected lookup, legacy-group inventory and broader cutover remain unfinished.
- [ ] Replace kick/ban/move and management power comparisons with capabilities plus target hierarchy; inventory sensitive recording/address and token actions explicitly.
- [x] Propagate the negotiated native authorization model through login, reconnect and scoped tab activation. Suppress retired reads/events while identity is pending; disable legacy privilege-key controls and guard retained group/grid/copy/appearance callbacks in role mode. Show current own roles from snapshots. Freeze tray reconnect status/source/destination and discard obsolete failures, including cancellation of late fallback guest connections. Legacy-to-legacy native group API inventory, storage retirement and full activation remain unfinished; see docs/native-api-inventory.md.
- [ ] Use revision-aware cache invalidation and committed policy changes to revalidate active affected sessions, not just refresh controls.
- [ ] Verify: lower/equal/higher targets, Administrator ownership boundaries, backend failure and concurrent revocation tests.

Dependencies: 11, 4. Scope: medium per moderation/cache slice. Likely files: `internal/server/admin.go`, `perms.go`, new authorization service/cache, tests.

## 13. Align integrations and version negotiation
- [x] Add a gRPC socket-completion boundary for protected unary reads, retaining policy/metadata after handler return through final written frames or joined connection closure. Enable ListChannels with native filtered projection and current admission; verify partial/continued headers, real socket stalls, zero HTTP/2 window, deadline closure, callback shutdown joining and PostgreSQL same-connection revocation.
- [x] Add Query/SSH serverinfo/clientinfo/serverconfig with shared native filtering, current admission and bounded protected delivery. Keep connection statistics and addresses independently gated, omit hidden populations/MOTD and require ManageServer for configuration; preserve native self-session behavior without granting integration self exemptions.
- [x] Add bounded Query/SSH banquery recovery reads with current BanMembers/admission, descending ID pagination and policy/metadata retained through delivery. Verify real transports and PostgreSQL pagination, concurrent inserts, grant revocation, expiry/issuer fields and sanitized denials.
- [x] Add typed gRPC GetServerInfo/GetClientInfo/GetServerConfig/ListBans through the native protected callbacks. Preserve field filtering, signed sentinel values, global gates, current admission and Unix-second ban pagination; verify real transport and PostgreSQL reads.
- [x] Add typed gRPC GetRoleState/ListRoleMembers/CheckAccess/GetChannelOptions through native protected callbacks. Preserve scoped projection and hidden-parent filtering, revision-aware offline roster, subject decisions separate from actor hierarchy, and lifecycle preflight options; verify current admission and revocation over real transport/PostgreSQL.
- [x] Add gRPC ListClients/GetChannelInfo through native filtered snapshots, matching Query discovery fields and protected delivery. Verify visible unassigned sessions, hidden/invisible members, flattened parents, current revocation and admission.
- [x] Inventory every current legacy Query command and existing gRPC/HTTP surface; verify all 33 unsupported legacy Query commands cannot reach their backend handlers in role mode. Keep missing adapters and cutover decisions explicit in docs/integration-operation-inventory.md.
- [x] Add Query/SSH auditquery and gRPC ListAuditLog using the shared native audit projection. Retain trusted scope provenance, redacted pagination, owner history access, current ViewAuditLog/admission and policy/metadata through bounded delivery; verify native/PostgreSQL and actual transports.
- [x] Add Query/SSH complaintquery/complaintclear and gRPC ListComplaints/ClearComplaints with native BanMembers, bounded oldest-first pages, current admission, target/reporter deletion and canonical committed audit. Verify PostgreSQL cursor/scope/revocation, real transports and cancellation. Legacy single-ID/global deletion semantics remain an explicit inventory decision.
- [x] Add Query/SSH rulesquery and gRPC GetServerRules with current ManageServer/admission and protected delivery. Count acceptances for exactly the returned wording/hash; verify concurrent wording replacement, current grants/revocation, native/PostgreSQL and real transports.
- [x] Add Query/SSH serverconfigset and gRPC SetServerConfig with shared native validation, atomic six-setting persistence, serialized runtime publication and canonical audit. Reproduce/fix concurrent native save reordering and explicit legacy reply deadlines; verify cancellation, rollback, current authority, native restart values and real transports.
- [x] Add a capability-advertised desktop media-limit editor plus native, Query/SSH and gRPC read/write contracts. Require complete bitrate/width/height replacements, current ManageServer, coordinated persistence/publication and exact revision acknowledgements; verify old-server compatibility, revocation and PostgreSQL restart values.
- [x] Add Query/SSH chatfilterquery/chatfilterset and gRPC GetChatFilters/SetChatFilters with separate ManageChatFilters, current admission and protected delivery. Preserve omitted/cleared lists, stop partial writes on failed/corrupt reads, serialize native/legacy cache publication and verify normalization, moderation effects, cancellation, PostgreSQL authority/audit and actual transports.
- [x] Add Query/SSH servertextset and gRPC SetServerText for name/MOTD/announcement/rules with current ManageServer/admission, explicit clearing, encrypted broadcast text and canonical no-plaintext audit. Verify re-seal/write ordering, cancellation, current authority, restart decryption, rules acceptance and actual transports.
- [x] Replace integration custom annotations with Query/SSH customquery/customchange and gRPC ListCustomMetadata/ChangeCustomMetadata under current ManageServer/admission. Preserve opaque/orphan subjects, bound pages and legacy field transfer, distinguish empty values from exact-key deletion, and omit values/raw keys from audits. Verify PostgreSQL storage/scope/revocation/cancellation and actual transport contracts.
- [x] Add explicit default-disabled integration eligibility, canonical opaque identities and per-operation account/ban validation. Provision new identities with `adduser -integration` without granting roles or changing duplicate accounts.
- [x] Route Query/SSH clientlist, channellist and channelinfo through current filtered native snapshots, retaining policy/metadata through bounded writes; reject unsupported commands without legacy fallback. Validate PostgreSQL identity checks, same-session access changes, actual TCP/SSH reads and slow SSH readers.
- [x] Add Query/SSH rolelist and revision-checked rolechange using the native role projection, transactional mutation/audit and hierarchy. Revalidate admission inside the mutation barrier, bound nested projections and preserve committed acknowledgements after reconciliation timeout. Protect parent policies while keeping child access edits functional.
- [x] Add gRPC integration login and typed ChangeRoles using the shared native authority; preserve legacy-mode behavior and close unsupported role-mode RPCs/events. Validate actual PostgreSQL authentication, canonical identity, audit actor, stale revisions, disabled accounts and committed pending enforcement over gRPC.
- [x] Add Query/SSH rolemembers and accesscheck with strict revision-aware requests, native roster/decision semantics and bounded protected delivery; verify protected/offline identities, hierarchy explanations, stale revisions, current eligibility and pending-enforcement denial.
- [x] Add Query/SSH channelquery/channelchange and typed gRPC ChangeChannel using the shared native lifecycle, including creation preflight, admission revalidation under the writer barrier, access-preserving moves, atomic resource/policy/audit commits and acknowledged reconciliation.
- [x] Add Query/SSH membervoice/membermove and typed gRPC SetMemberVoice/MoveMember using shared native moderation and movement. Preserve optional flags, current admission, hierarchy, invisible-target privacy, both move scopes, target Connect, capacity, session voice state and canonical scoped audit. Verify real transport and PostgreSQL admission/media effects.
- [x] Add Query/SSH memberdisconnect and typed gRPC DisconnectMember sharing the native channel-leave lifecycle, matching the displayed target scope, preserving session state/subscriptions, normalizing inaccessible targets and auditing the former channel. Bound notification writer acquisition/delivery and retain a separate bounded audit attempt for committed disconnects.
- [x] Add Query/SSH memberkick/memberban and typed gRPC KickMember/BanMember with admission inside the exclusive barrier and native teardown. Return distinct persistence and cleanup outcomes; verify actual transports, PostgreSQL admission after waiting, active-file quiescence, canonical audits and unconfirmed INSERT recovery.
- [ ] Wire Query/SSH and gRPC through the same authorization service, defining service/operator identities explicitly; replace old group/permission commands or reject unsupported commands.
- [ ] Advertise the new authorization model in client/server handshake; incompatible peers receive an upgrade error before protected operations.
- [x] Implement native/desktop roles-v1 negotiation, signed-challenge invalidation, Query login and per-channel SSH negotiation, and per-RPC gRPC model checks. Keep legacy mode unchanged; bundled-tool scenario coverage is tracked separately. See docs/authorization-model-negotiation.md.
- [x] Add explicit roles-v1 load-tool selection, encrypted and correlated global-chat confirmation, observed role channel membership, bounded key rotations through setup and server-error failure accounting. Validate real role-enabled account/guest allow/deny traffic; full legacy E2E scenario replacement and larger media rehearsal remain open.
- [x] Prepare gRPC per-message protected delivery for role event streams: retain callback authority through actual socket completion, bound/abort stalled sends, parse DATA without retaining bodies, and clean idle mappings without waiting for other writes. Verify real TCP delivery/revocation/cancellation and repeated idle streams. Public event filtering/admission/negotiation and HTTP event replacement remain open.
- [x] Add role-aware gRPC subscriptions using shared current-state projections, explicit model/admission checks, mandatory discovery/activity snapshots, filtered speaking deltas, hidden-event suppression and idle revocation refresh. Verify real PostgreSQL/TCP mute without VAD, source/viewer revocation, admission disablement, negotiation and subscription lifetime/capacity. HTTP event replacement and broader E2E coverage remain open.
- [x] Add a role-aware HTTP WebSocket event handler using the same projection/schema and current protected callbacks as gRPC. Preserve legacy selection, login/origin limits and bound the upgrade, frame writes and connection lifetime. Verify PostgreSQL parity, dropped-event closure, slot cleanup and authority retained until a canceled socket write has joined. Broader bundled E2E scenarios remain open.
- [x] Prepare E2E native/Query transport helpers for explicit model selection, setup key rotation, exact filtered-snapshot membership and acknowledged JSON responses. Keep the CLI role profile unavailable until its legacy scenario replacements are implemented and validated.
- [x] Add internal E2E owner/fixture preflight, acknowledged role/channel lifecycle, delegated hierarchy and same-session revocation, guest/member access explanations, revision conflicts and guarded cleanup. Validate through real Query/Authority/PostgreSQL with restored policy and audit evidence. Full CLI profile/native traffic integration remains open; see docs/role-e2e-testing.md.
- [x] Exercise native role nickname/account/guest auth, filtered membership, correlated encrypted channel/global/direct traffic, history/edit/delete, concealed non-author deletion, live send denial/member allow and slow-mode recovery against PostgreSQL. Prove rejected sends leave history and recipient delivery unchanged. File scenarios, full CLI profile and complete runtime reconciliation remain open.
- [x] Expose the explicit roles-v1 E2E profile with health/UDP, owner/account/baseline/login preflight before policy writes, stop-on-failure, existing role/channel/native scenarios, file byte round trips and permanent token revocation, and semantic policy restoration. Run the full profile against real listeners/PostgreSQL; verify failed login leaves policy unchanged. Broader Query/event parity, chaos and complete runtime reconciliation remain open; see docs/role-e2e-testing.md.
- [ ] Verify: transport parity, legacy client/command rejection and complete client-module/generated-binding builds.

Dependencies: 10–12. Scope: medium per adapter; use sequential commits for Query, gRPC and native handshake. Likely seams: `internal/query`, `internal/grpcserver`, `internal/server/queryadmin.go`, `internal/netproto`, `client`.

## Checkpoint D: enforcement coverage
- [ ] Every action from Task 1 has a tested new authorization path. No active legacy fallback or implicit current-channel scope remains in the replacement mode.

## 14. Rehearse fresh setup and activation
- [x] Add a read-only non-serving owner/setup inspection command with exact unique-ID selection, explicit stored activation state, legacy authority counts and incomplete/invalid staging reports. Require an existing schema; never migrate or print credential material. Verify read-only PostgreSQL execution and unchanged fixture table digests. Reject orphan staging atomically in the preparation helper and verify concurrent preparation/audit rollback. This preflight does not perform reset, verify owner recovery or activate enforcement; see docs/role-setup-preflight.md.
- [x] Implement explicit operator setup selecting an exact owner, seeding a closed roles-v1 baseline, rotating all current scope keys and activating atomically. Production startup reads only the active role policy.
- [x] Require a fresh-version install marker in production commands; refuse unmarked databases with existing public tables.
- [ ] Rehearse fresh schema initialization, transactional activation, interrupted setup, scope-key rotation and backup restoration on a disposable database.
- [ ] Verify: actual PostgreSQL setup tests, no unauthorized channel visibility and owner recovery; do not count skipped DB tests as passing.

Dependencies: 13, Checkpoint D. Scope: medium per reset/rehearsal slice. Likely files: setup CLI, reset store methods/tests, server startup and operator runbook.

## 15. Remove the retired permission system
- [ ] Remove raw grid/tier APIs, group grant automation and TS3 power/skip/negate evaluation; preserve unrelated audit/ban features formerly in `perms-ui.js`.
- [x] Remove retired authority columns/tables from the resulting fresh schema; old databases are rejected rather than upgraded.
- [ ] Verify: enforcement inventory has no unresolved legacy dependency; run root/client Go suites, targeted race tests, frontend unit/browser/lint/build and protocol compatibility checks.

Dependencies: 14. Scope: multiple small cleanup commits by layer; do not mix enforcement changes into bulk deletion.

## Final checkpoint
- [ ] Human review of role/access UX and threat scenarios completed.
- [ ] Owner identity, backup, maintenance window and restore procedure are concrete deployment inputs.
- [ ] Fresh database, owner credentials and backup restoration have been reviewed.
- [ ] Production activation happens as a separate explicit operational step, after development checks and rehearsal.
